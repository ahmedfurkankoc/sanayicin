# Admin Panel Environment Variables

# Backend origin used by Next.js rewrites (no trailing slash)
# For local development:
# BACKEND_ORIGIN=http://localhost:8000
BACKEND_ORIGIN=http://localhost:8000

# API base used by the app. Keep as same-origin path so HttpOnly cookie works
NEXT_PUBLIC_API_URL=/api/admin

# Environment
NODE_ENV=development
